<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uclk.shani.youseelk">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAR7FHHIhk9r1tdk6CGs2HqT3zbCgpG6v8" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:label="Sign Up" />
        <activity
            android:name=".activities.PreferencesActivity"
            android:label="Preferences" />
        <activity
            android:name=".activities.SelectLocationsActivity"
            android:label="Locations" />
        <activity
            android:name=".activities.DurationActivity"
            android:label="Durations"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.MainTourActivity"
            android:label="Tour Schedule" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="Settings" />
        <activity android:name=".activities.AboutActivity" />
        <activity
            android:name=".activities.MyPlansActivity"
            android:label="My Plans"/>
        <activity
            android:name=".activities.FinalPlanActivity"
            android:label="Tour Plan" />
        <activity
            android:name=".activities.ViewActivity"
            android:label="View Panorama" />
        <activity
            android:name=".activities.SavedPlanView"
            android:label="My Plans"/>
        <activity
            android:name=".activities.EditPlan"
            android:label="Edit Plan"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity
            android:name=".activities.MapActivity"
            android:label="Map and Directions"/>

        <activity android:name=".activities.SavePlanAs"
            android:label="Save plan as.."
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>

    </application>

</manifest>